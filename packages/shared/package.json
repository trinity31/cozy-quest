{
  "name": "@cozy-quest/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(shared) no lint configured\"",
    "build": "echo \"(shared) consumed via transpilePackages\""
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
